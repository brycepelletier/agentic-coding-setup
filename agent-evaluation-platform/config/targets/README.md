# Inference targets

Keep user-specific target files here. Copy `targets.example.json` to a filename
such as `local-models.json`, then pass it with `--targets`. Target definitions
remain data; they are not embedded in runner source.
