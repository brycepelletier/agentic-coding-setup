# Evaluation fixtures

Level 5–8 fixture workspaces are materialized per run from the public repositories
and commits pinned in [`../config/execution-contracts.json`](../config/execution-contracts.json).
This directory intentionally contains no mutable shared checkout. See
[`../config/EXECUTION.md`](../config/EXECUTION.md) for the capability audit,
tool boundary, fixture identity, and historical-result policy.
